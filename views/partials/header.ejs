<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>
    <%= title %>
  </title>
  <!-- Bootstrap CSS via CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/vendor/fontawesome-free/css/all.min.css" rel="stylesheet" type="text/css">
  <link href="/vendor/datatables/dataTables.bootstrap4.min.css" rel="stylesheet">

  <link href="/css/sb-admin-2.css" rel="stylesheet">
  <script src="/js/permissions.js"></script>
</head>

<style>
  .b-example-divider {
    height: 3rem;
    background-color: rgba(0, 0, 0, .1);
    border: solid rgba(0, 0, 0, .15);
    border-width: 1px 0;
    box-shadow: inset 0 .5em 1.5em rgba(0, 0, 0, .1), inset 0 .125em .5em rgba(0, 0, 0, .15);
  }

  .form-control-dark {
    color: #fff;
    background-color: var(--bs-dark);
    border-color: var(--bs-gray);
  }

  .form-control-dark:focus {
    color: #fff;
    background-color: var(--bs-dark);
    border-color: #fff;
    box-shadow: 0 0 0 .25rem rgba(255, 255, 255, .25);
  }

  .container a img {
    width: 100px;
  }

  .bi {
    vertical-align: -.125em;
    fill: currentColor;
  }

  .text-small {
    font-size: 85%;
  }

  .dropdown-toggle {
    outline: 0;
  }
</style>

<body>
  <header class="p-3 bg-light text-dark">
    <div class="container">
      <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
        <a href="/" class="d-flex align-items-center mb-2 mb-lg-0 text-white text-decoration-none">
          <img src="/img/logo-removebg-preview.png" alt="">
        </a>

        <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
          <li>
            <a href="/" class="nav-link px-2 <%= activePage === 'home' ? 'text-secondary' : 'text-dark' %>">
              Inicio
            </a>
          </li>

          <!-- Reemplazado el ítem 'Proyectos' por dropdown 'Gestión' -->
          <% if (user && (user.cod_ascinsa==='EJQ001' || user.area==='004' )) { %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle px-2 <%= activePage === 'gestion' ? 'text-secondary' : 'text-dark' %>" href="#" id="gestionDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Gestión
            </a>
            <ul class="dropdown-menu" aria-labelledby="gestionDropdown">
              <li><a class="dropdown-item" href="/projects/cartera1">Cartera #1</a></li>
              <li><a class="dropdown-item" href="/projects/cartera2">Cartera #2</a></li>
              <li><a class="dropdown-item" href="/projects/proyectos-ti">Proyectos TI</a></li>
              <li><a class="dropdown-item" href="/projects/isco-cargo">ISCO Cargo</a></li>
            </ul>
          </li>
          <% } %>

          <% if (user && (user.area==='004')) { %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle px-2 <%= activePage === 'reports' ? 'text-secondary' : 'text-dark' %>" href="#" id="reportsDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Requerimientos
            </a>
            <ul class="dropdown-menu" aria-labelledby="reportsDropdown">
              <li><a href="/reports/global" class="dropdown-item">Reporte de Requerimientos Globales</a></li>
              <li><a href="/reports/my" class="dropdown-item">Reporte de Requerimientos Individuales</a></li>
            </ul>
          </li>
          <% } %>

          <!-- Only show Registro de Horas for users with area '004' -->
          <% if (user && user.area==="004" ) { %>
          <li>
            <a href="/hour/view-hours" class="nav-link px-2 <%= activePage === 'view-hours' ? 'text-secondary' : 'text-dark' %>">
              Registro de Horas
            </a>
          </li>
          <% if (user && (user.cod_ascinsa==='EJQ001' )) { %>
          </li>


          <% if (user && (user.area==='004')) { %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle px-2 <%= activePage === 'reports' ? 'text-secondary' : 'text-dark' %>" href="#" id="reportsDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Control
            </a>
            <ul class="dropdown-menu" aria-labelledby="reportsDropdown">
              <li><a href="/control" class="dropdown-item">Control Interno</a></li>
              <li><a href="/checking" class="dropdown-item">Control de Equipos</a></li>
            </ul>
          </li>
          <% } %>
          <li>
            <a href="/history" class="nav-link px-2 <%= activePage === 'history' ? 'text-secondary' : 'text-dark' %>">
              Historial
            </a>
          </li>
          <% } %>
          <li>
            <a href="/docs" class="nav-link px-2 <%= activePage === 'docs' ? 'text-secondary' : 'text-dark' %>">
              Documentación
            </a>
          </li>
          <% } %>
        </ul>
        <div class="text-end">
          <a href="/auth/logout" class="nav-link px-2 text-dark">Cerrar Sesión</a>
        </div>
      </div>
    </div>
  </header>
  <div class="container mt-4">
</body>

</html>